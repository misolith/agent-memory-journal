# v0.2.1 Issue List (Post-v0.2.0 Refinement)

The following items are identified as remaining tasks or refinements following the successful v0.2.0 3-tier architecture implementation.

## Documentation
- [ ] **docs/glossary.md**: Define terms like `pinned`, `supersedes`, `decay`, `ingest`, and `tier`.
- [ ] **docs/open_questions.md**: Document architectural trade-offs (e.g., BM25 vs Vector) and unresolved design decisions.
- [ ] **Session Lifecycle**: Formally define when a session is considered "closed" and when `.memory/sessions/` files should be archived or deleted.

## Core Logic & Refinement
- [ ] **Semantic Deduplication (6.2)**: Implement similarity-based deduplication in the promotion pipeline (currently uses token overlap).
- [ ] **Last Seen Metadata**: Add `last_seen:<timestamp>` to core memory items to track usage/reference frequency (beyond just `created`).
- [ ] **Supersedes Validation**: Ensure that a `supersedes` link actually points to a valid, existing ID.
- [ ] **Recall Index Caching**: Implement a local cache for the BM25 index to avoid re-reading all episodic files on every query as the history grows.

## CLI / UX
- [ ] **Explicit Legacy Export**: Ensure `LegacyJournal` can be imported directly via `from agent_memory import LegacyJournal` or clearly document its location in `agent_memory.api`.
- [ ] **Doctor Fixes**: Allow `doctor --fix` to automatically regenerate manifest mismatches if the user confirms.

## Maintenance
- [ ] **Outdated Content Audit**: Review `core/` files for remaining test data or placeholder notes that should be superseded or archived.
